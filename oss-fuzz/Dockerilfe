# OSS-Fuzz 빌드 컨테이너 규약(런타임은 Buttercup/ClusterFuzz가 준비)
FROM gcr.io/oss-fuzz-base/base-builder:bookworm

# 프로젝트 소스 복사
COPY . $SRC/example-ini-parser
WORKDIR $SRC/example-ini-parser

# 의존 설치(필요 시)
RUN apt-get update && apt-get install -y --no-install-recommends \
      cmake make gcc g++ \
  && rm -rf /var/lib/apt/lists/*

# build.sh가 실제 빌드를 수행
COPY oss-fuzz/build.sh $SRC/
